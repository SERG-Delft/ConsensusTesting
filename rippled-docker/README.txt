How to create a docker image
1. Create a dockerfile and entrypoint file in a folder (make sure unix (lf) line endings are used)
2. Use the following command from inside this folder 'docker build --tag [tag_name] ${pwd}/.'
3. Wait for 4 and a half hours.